const levelup = require('levelup');
const leveldown = require('leveldb-mcpe');
const nbt = require('prismarine-nbt');

// Since we don't have a db, this script would just test compilation
console.log("Compiles");
